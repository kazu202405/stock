# 次にやること（2026-09-02 時点・五島さんと合意）

このファイルは作業再開時に最初に読む。上から順に片づける。

## 0. まず確認（作業ではない・5秒）

明日の 9:25 以降、`/dashboard/admin` の鮮度パネル（または `job_runs`）で
**`price_update:start` が1件だけ**かを見る。3件のままなら、Render の
Start Command を直したあとも worker が複数立っている（＝Yahooへ3倍投げている）。

## 1. 🟡 業績予想 81% → TDnet へ移す

いちばん脆い Yahoo日本版HTML に業績予想（売上・営業益）が乗っている。
TDnetの決算短信に載っているので移せる。⚠️ 無料閲覧は**直近31日ぶん**だけなので、
毎日拾い続ける運用が要る（既存の決算検知cronに相乗りできる）。

**株探依存の差し替えと同じ道具**なので、まとめてやると効率がいい
（決算検知 `earnings_scraper.py` が毎日15:30/21:00に株探を叩いている。
方針は「アプリに載せる値の取得元にしない」だが、代替ができるまで現行運用）。

## 2. 🟡 決算月の食い違い44件を判定する

yfinance と EDINET で食い違う（例: 3612 ワールド いま3月 / EDINET 2月）。
**有報の対象決算期（periodEnd）を見れば正解が分かる**ので、EDINET導入済みの
いまなら判定できる。放置すると決算情報ページの銘柄一覧が間違ったまま。
一覧は `edinet_codes.fiscal_month_mismatches()` で出る。

## 3. 🟢 死んでいる列の掃除

- `business_summary`（英語概要）0.6% … 日本語概要100%が置き換え済み
- `major_holders`（英語の大株主）0.6% … `major_shareholders_jp` 99.8%が置き換え済み

どちらも取得経路が死んでいる。消すか「使わない」と明記するか。
放置すると次に見た人が「壊れている」と誤解する。

## 4. 🟢 `/health/db` の詳細漏れ

DB接続エラー時に `detail: str(e)` を返している。**未ログインで叩ける口**なので、
例外文から内部構成が見える。2026-09-01に触った場所。

---

# 未完了の作業（2026-08-25 更新）

> **⚠️ 2026-08-25 以降のセッションはここから読む。**

## いまの状態（2026-08-25 深夜）

**走っているものは無い。git は clean、すべてプッシュ済み。テスト647件パス。**
（朝は402件。今日 +245件 / 39コミット）

| 項目 | 充足率 |
|---|---:|
| 有利子負債 / 利益剰余金 | 89% / 95% |
| 総資産 / 純資産 | 89% / 89% |
| 日足の出来高 | 99% |
| 信用買残・売残 / 信用倍率 | 94.6% / 59.6% |
| 売上の増減率 / 営業利益の増減率 / 流動比率 | 97.9% / 86.0% / 95.7% |
| 株主 / 設立日 / 業績予想 | 64.3% / 66.1% / 62.6% |

⚠️ **信用倍率が59.6%なのは壊れているのではない。** 売残ゼロの銘柄が1,357件
あり、そこでは倍率が定義できない（0で割れない）。**回転日数のほうが広く出せる**
（買残があれば出るので94.6%）うえ、意味も取りやすい。

⚠️ **Yahoo!JP から遮断されている**（株主・設立日・業績予想の3項目）。
sleep を10秒にしても改善しなかったので速度ではなく時間帯のペナルティ。
夜間ジョブ（毎晩2:00・60件）に任せる。

---

## 2026-08-27〜28 の作業（すべてプッシュ済み）

`dc7e254` `43ffac7` `3d6eb4e` `55cc2e0` `2b7a840`。テスト 701 → **717件**。

### 好調企業・テクニカルが「読み込み中」のまま止まっていた
`/dashboard` と `/dashboard/admin` で**タブの数が違う**（4 vs 6。GC・DCは管理画面のみ）。
`activeTab` は sessionStorage なのでページをまたいで残るため、管理画面でGCタブを
開いたあと `/dashboard` に来ると、無いタブの読み込みが走って描画が止まっていた。
⚠️ **「一般会員ではボタンが無いから」という説明は誤り。**
   `wlAnalyzeBtn`・`techAnalyzeBtn` は **/dashboard には管理者でも存在しない**。
   会員と管理者で /dashboard の要素数は 105 個で完全に一致する。
   ここを取り違えると「自分は管理者だから関係ない」と読み飛ばす。

### セッション署名鍵（`43ffac7`）
`config.py` が2か所で設定し、後の `config['SECRET_KEY']` が勝っていた。
`APP_SECRET_KEY` は設定しても使われない状態。既定値 `'secret-key'` は公開文字列で、
**その鍵でセッションを作れば管理者として全APIを通せた**（診断で実証）。
本番（Render）には設定済みで実害なし。1か所に集約し、既定値なら起動を止める。

### 未ログインで通る書き込みAPI 3本（`55cc2e0`）
    POST /api/stock/batch                   最大200銘柄を外部APIで分析
    POST /api/dc-stocks/scrape              叩いたら実際に52件スクレイプして返した
    POST /api/technical-stocks/analyze/stop 走っている分析を止められる
🔴 **テスト自体がフェイルオープンしていた。** `test_write_endpoints_guarded` は
   関数本文を「宣言から40行」固定で読んでおり、本文11行の
   `/api/dc-stocks/scrape` では窓が次の関数まで食い込んで、
   **隣が守られているだけで合格**していた。次の `@app.` / `def` で打ち切るよう修正。

### データ鮮度パネル（`2b7a840`）
定期実行14本に通知が1つも無く、止まっても気づけなかった。
`/dashboard/admin` の先頭に、**最後に実際に値が動いた実績**を出す
（`/api/scheduler/status` の「次回実行時刻」は空振りでも正常に見えるので使わない）。
⚠️ 誤検知を出さないための決め事（出すと誰も見なくなる）:
  - 母数から PRO Market・ETF・REIT・廃止を外す
  - 決算の再分析は「古さ」で判定しない（決算が無い時期は何もしないのが正常）
  - 株価は最古1件でなく「古い銘柄が何件あるか」で判定
  - 経過日数は**営業日**で数える（暦日だと月曜の朝に必ず警告が出る）
  - GC/DCは `ma_crosses.calculated_at`。**`signal_stocks` ではない**
    （旧kabutan経路。取り違えて「22営業日前」と誤検知した）

---

## いまの数字（2026-08-30 実測・内国普通株3,669件）

    株価が1営業日以内   99.2%      会社概要      87.2%
    現預金 99.8%       利益剰余金 99.7%   有利子負債 93.0%
    eps列と履歴のズレ 0件          4営業日超の株価 0件（廃止手前を除く）

自動更新は本番で動いていることをデータで確認済み（土日をまたいでも
会社概要・GC/DCが 8/30 付で更新されている）。

---

## 残っていること

1. **`gia2018.com/upgrade`・`/plans` の文言** — 8/12から動いていない。
   **売上への効きが一番大きい残件**（コードではなく言葉の仕事）
2. **勉強会に実物を入れる** — 器はできた。動画1本・資料1つで通し確認したい
3. **kabutan 依存の差し替え** — `gc_scraper.py:117` / `app.py` の技術銘柄一覧 /
   `earnings_scraper.py`。消すと ETF・REIT 除外が壊れるので
   「`screened_latest` の値に差し替える」
4. **`/api/stock/holders-officers/<code>` に取得の上限が無い**（503の芽）
5. **`role_required('admin')` のねじれ** — `app_users.role` しか見ないので、
   メール基準の管理者だと `/api/admin/*` で403になる。運用系22本は
   `admin_required_api` に置き換え済みなので、残りはこの系統だけ
6. **未同期の102件**（`price_updated_at` が NULL）— 再分析すれば直る
7. **スコアに「有利子負債 ≤ 純利益×10」を足すか**（保留・数字の裏付けは下記）
8. ~~売買が止まっている銘柄94件~~ → **誤りだった（2026-08-26 訂正）**
   103件すべてJPXの公式一覧で **PRO Market**（TOKYO PRO Market）だった。
   プロ投資家向け市場なので**売買が成立しない日が続くのが正常**で、
   Yahoo・kabutanの対象外なのも当然。上場廃止ではない。
   ⚠️ 判定を3回間違えた。記録しておく:
     (1) `b.get('volume') or 0` で「キーが無い」を「出来高ゼロ」と読み、
         松井建設（プライム・427億）まで廃止と判定した
     (2) Yahoo・kabutanが404を返すのを「廃止」の証拠と読んだ。
         実際は**両社がPRO Marketを扱っていない**だけ
     (3) `financial_history` の並び順が銘柄ごとに違うのに `[-1]` で
         「最新」を取り、876件が壊れていると誤判定した
   **教訓: 市場区分を持たずに「異常」を判定しない。**
   正解は JPX の `data_j.xls`（`jpx_master.py` に取得済みの仕組みがある）。

9. ~~eps 列が履歴より1期古い~~ → **完了（2026-08-26 `b25a540`）**
   1,324銘柄を修正。`supabase_client.sync_history_scalars()` を upsert と
   update の両方に入れ、履歴を書くときは列も作り直すようにした。
   ⚠️ 列ごとにルールが違う。dps・payout_ratio は**進行中の年度を除く**
      （期末前の中間配当を年間配当として拾うと1株配当が半分になる）。
      eps と同じルールに揃えると配当が壊れる。

10. ~~JPXの一覧に無い45件~~ → **一部完了（2026-08-26 `9d57712`）**
   18件に上場廃止の印を付けた（21 → 39件）。
   `detect_delisted.py` を3段構えにし、JPXの公式一覧を見るようにした。
   ⚠️ `probe_is_alive` は**廃止から1年経つまで「上場中」を返す**。
      これだけで判断すると、正しく付いた印まで外す。
   **残り:**
     - 保留6件（2763 / 311A / 3271 / 4367 / 6096 レアジョブ）は
       最終足が27〜29日前。あと数日で自動的に候補に入る
     - **4449 ギフティは廃止ではなく `590A` ギフティグループへのコード変更。**
       「廃止」と「コードが変わった」を区別する仕組みが無い（未着手）
     - 136A は `company_name` が `136A.T,0P0001SALU,0` に壊れたまま
       （廃止印が付いたので画面には出ない）

11. ~~内国普通株でないものが172件~~ → **完了（2026-08-26 `c2af906`）**
   `exclude_non_operating()` が市場区分でも外すようになり、漏れていた
   8963 インヴィンシブル投資法人・8987 Japan Excellent を捕捉。
   5桁の数字コード＝種類株式を除外（companies.json から8件削除）。
   ⚠️ PRO Market・外国株・出資証券は**事業会社なので除外しない**（既存方針）。
      PRO Market には銘柄ページで注記を出すようにした。

12. ~~market / market_segment 列がほぼ空~~ → **完了（2026-08-26 `c2af906`）**
   176件に反映。空欄は45→37件（JPXの一覧に無いもの）。
   `sync_market_segments.py` を月1で流せば追随できる。

13. **株価の一括取得に取りこぼしのムラがある**（2026-08-28 発見）
   `yf.download` が回によって数銘柄を落とす。次の実行で拾い直すので実害は小さいが、
   9691 両毛システムズは yfinance に値があるのに保存されていなかった。
   鮮度パネルに出るようになったので、頻発するようなら原因を追う。

14. **`AAPL` が銘柄一覧に入っている**（2026-08-26 発見・未着手）
   日本株のDBに米国株のコードが1件だけある。JPXの一覧にも無い。
   どこから入ったかは未調査。

---

## 2026-08-25 夜の作業（すべてプッシュ済み）

### 12. 総資産・純資産（`d09cc5e` `08990a9`）

`backfill_balance_debt.py` に2行足すだけだった（すでに読んでいる貸借対照表の中に
`Total Assets` / `Stockholders Equity` がある。追加リクエストはゼロ）。

**PBRの検算手段ができた。** 2026-08-12 に「時価総額÷純資産なら株数を経由しない
ので検出できるが、`equity` 列は全3,879件で空」と詰まっていたものが解けた。

  検算できた 3,458銘柄（89.6%） / 1.5倍以上ずれている 47件（1.4%）
    PBR>=10で不合格だが実際は合格 … **1件**（4052 フィーチャ 11.09 → 4.07）
    PBR<10で合格だが実際は不合格 … 2件

8/12に懸念された「13銘柄が誤判定で減点」は**1件**まで減っていた。
道具は `check_pbr_consistency.py`（読むだけ・書き込まない）。

⚠️ **円建てでない純資産を億円として保存しない。** 3銘柄で「時価総額÷純資産」が
150〜460倍になった（6269 三井海洋開発 / 4875 メディシノバ / 7699 オムニ・プラス）。
**通貨のメタ情報（financialCurrency）は信じない**——6269 は JPY と申告している
のに実際はドル建てだった。数字そのもので検算して弾く（境目50倍。実測で
10〜50倍が0件という谷がある）。
⚠️ 一番たちが悪いのは**自己資本比率とは整合してしまう**こと（純資産÷総資産は
通貨に関係なく正しい）。片方が正しいので壊れて見えない。

### 13. 今期予想の手入力（`d09cc5e`）

取得できるのは6割ほど。決算短信を見れば分かるので手で入れられるようにした。

⚠️ **経常利益は必須にしない。** IFRSを適用している会社には経常利益という区分が
無く、実測160社（トヨタ含む）が売上・営業・純利益だけを持っている。4つ必須に
するとその会社の予想が画面から消える。銘柄ページは**3つ揃ったときだけ**
予想の行を出す（1つ2つしか無い行は「減収に見える」を作る）。

### 14. 「この会社のノート」と事業概要の手直し（`f5989a4` `3d8a7c7`）

企業分析の置き場所を器で決めた:
  business_summary_jp … **一行紹介**（中央値85文字・最長184文字）。3か所に出る
  community_questions … **「問い」**（answer_count / is_resolved を持つ）
  notes … **「記事」**（company_code / is_public / poster_name）← ここ

⚠️ **投稿者名をノートごとに焼き付けない。** 表示名を変えると過去のノートが古い
名前のまま残り、**同じ人が何人もいるように見える**。アカウントの表示名に一本化。
⚠️ **公開の同意文を実態に合わせる。** 「コミュニティに公開」のままだと、
書いた人が同意した範囲（会員が見る）と実際に出る範囲（誰でも見る）がずれる。

### 15. 🔴 コミュニティで投稿名を入れると投稿できなかった（`71f80b4`）

`community_questions` / `community_answers` に **poster_name 列が無い**のに、
画面には入力欄があり、コードは値があれば insert に混ぜていた。PostgREST は
PGRST204 を返すので**投稿名を書いた人は質問も回答もできなかった**
（質問1件・回答0件だった理由の可能性）。経路ごと無くした。

### 16. 決算の取りこぼし検知（`64b64dc`）

決算の検知は kabutan のスクレイピング頼みで、漏れても**エラーが出ない**
（「検知しなかった」だけで処理は正常に終わる）。決算月は98%分かっているので
「期末から猶予を過ぎたのに最終分析日がその期末より前」を毎晩23:30に数え、
**見つけたら同じキューに積み直す**（数えるだけでは直らない）。

実行結果: 3,804銘柄中 **0件**。いまは検知が効いている。
⚠️ 猶予75日。短くすると「まだ出ていないだけ」を漏れと数える。
⚠️ 一晩20件まで。全部積むと翌日の再分析が何百件も走る。
⚠️ 鮮度は `analyzed_at`（`updated_at` は2月で止まっている行がある）。

### 17. 勉強会の動画・資料（`8cc09fb` `97ca979`）

**有料会員（4,980円〜）のみ。段による出し分けはしない**ので既存の
`member_required_api` をそのまま使う（**段の判定を新しく作らない**）。

  動画 … 外部URL（YouTubeの限定公開）を埋め込む
  資料 … Supabase Storage の**非公開バケット**（作成済み・50MB上限）

⚠️ **動画をバケットに置かない。** 1本1GBを50人が見れば50GBの転送になり費用が
読めなくなる。受け付ける種類から video/* を外してある。
⚠️ **ファイルのURLを保存しない。** 開くたびに1時間で切れるURLを発行する。
保存すると退会後も生きたURLを配る。バケット内のパスも画面に返さない。
⚠️ Drive のリンクを貼る案を採らなかったのは、**権限の管理がDrive側になる**から。

  管理: /admin/study-materials    会員: /learning の「勉強会」タブ
  **migration 適用済み**・通し確認済み（登録／公開／署名URL／削除）。

### 18. 押せないボタンの掃除とXSS（`9170d36`）

運用系APIを管理者限定にしたぶん、画面側の出し分けが追いついておらず、
**押すと403になるだけのボタンが8つ**一般会員に見えていた。
ダッシュボードの「詳細取得」「停止」「スコア再計算」、銘柄ページの「更新」、
スクリーナーの「削除」。

⚠️ ウォッチリストは**利用者ごとではなく全体で1つ**。1人が消すと全員から消える。

🔴 **外部由来の文章を innerHTML / |safe で出していた。** 事業概要は
①Yahooのページ ②OpenAIの出力 ③手入力 の3経路で入り、①②は中身を決められない。
`<script>` や `<img onerror>` が混じれば**公開ページで動く**
（プロンプトインジェクションの出口でもある）。

  html_safe.sanitize_rich_text … `<br>` だけ残して他はエスケープ
  **保存の入口と表示の直前の両方**に置く。片方だけだと片方を直したときに穴が開く。

実データに危険なタグは0件だった（`<br>` が2,497件だけ）が、経路は開いていた。

---

## 2026-08-25 の作業（すべてプッシュ済み・デプロイ済み）

18コミット。テスト402→520件。

### 1. 配当利回りを「予想」基準に統一（`c39cdda`）

同じ 367A が一覧で **5.93%**、銘柄ページで **4.08%** と出ていた。実績は決算期を
またぐと期末配当と翌期の中間配当が同じ12か月の窓に入って跳ね上がる。

**判定は `static/js/dividend-basis.js` が唯一の正**（`score-color.js` と同じ形）。
保留理由（8/14「予想が無い銘柄が並べ替えから消える」）は実測で消えていた:
**両方3,159件 / 実績だけ38件（全体の1%）/ 予想だけ22件**。

- 予想が主、無い銘柄だけ実績に落として「実」の印。**主役に印は付けない**
- **並べ替え・絞り込みも表示と同じ値**を見る（`DividendBasis.normalize()`）
- **GC/DC の表だけ実績のまま**（`gc_stocks` は kabutan 由来で予想を持たない）
- ⚠️ 管理者がカードの数字を直したときの書き戻し先が実績の列だった

### 2. 有利子負債・純有利子負債・利益剰余金（`c39cdda`）

- yfinance の `Total Debt` / `Retained Earnings`。**追加リクエストは1銘柄1回**
- 保存先は `cf_history`。`backfill_balance_debt.py` で完走（3,705件）
- **純有利子負債は保存しない。** 画面で「同じ決算期の現預金」と引き算する
- ⚠️ `Total Debt` は**リース債務を含む**。四季報とは細かく一致しない
- ⚠️ `Total Debt` の行が無い銘柄が**140件**。「取得元に収録されていません」と
  出す。**「無借金」とは書かない**
- ⚠️ **`Total Debt` は総負債ではない。** `stock_analyzer.py` の2箇所で総負債の
  候補に混ざっており、総資産が取れない銘柄で自己資本比率が実際より高く出た
  （367A なら 39.28% → 49.97%）

### 3〜4. 流動資産の行／出来高・売買代金・回転日数（`20d7094` `85028c3`）

「現預金37億 / 流動負債100億 / 流動比率125%」が並び、37÷100 で検算すると
合わなかった（分子の流動資産125億が画面に無い）。**並ぶ数字は検算できる状態に。**

カード名を「財務健全性」→**「資金・売買状況」**に変更。出来高は yfinance の
レスポンスに最初から入っているのに読まずに捨てていた（**追加リクエスト0**）。
直近20営業日の1日平均。売買代金は出来高×終値の概算。
⚠️ **流動性は日足からしか出さない**（週足の volume は期間の合計）。

### 5. 🔴 書き込み系APIが開いていた（`85028c3` `bf274fc`）

`/search` が `is_admin=True` を固定で渡し、保存先の `/api/watchlist/update` に
**認証が一切無かった**。未ログインで POST するだけで任意の銘柄の財務データを
上書きでき、`match_rate` も再計算されるのでスコアごと汚染された。

調べたら1本ではなく、**運用系のバッチ起動が軒並み開いていた**。
`admin_required_api` を新設し22本に適用。`/api/stock/analyze` と
`summary-jp` は会員限定。**`holders-officers` だけは公開ページが自動で叩く**
ので、非会員には保存済みだけ返し外部へは行かない形にした。

⚠️ **画面から編集欄を消すだけでは塞がらない。守るのはサーバー側。**
抜けの検知テスト = `tests/test_write_endpoints_guarded.py`。

### 6. 保存で財務履歴が消えていた（`a4c40ba`）

`financial_history` / `cf_history` を丸ごと置き換えており、編集画面の表に無い
キーが毎回消えていた（トヨタで `bps`、CF側は有利子負債・利益剰余金・流動資産）。
エラーは出ず「保存しました」と表示される。**キー単位のマージ**に変更。

### 7. `/search` の解体（`183085a` `a4c40ba` `4892469`）

6セクションのうち5つが `/stock/<code>` の重複だった。**移してから消した**
（逆順にすると移すまでのあいだ機能が無くなる）。

  銘柄検索       → ヘッダーの検索窓（`static/js/company-search.js`）
  5つの重複      → `/stock/<code>` に元からある
  管理者の手入力 → **`/admin/stock-data`**（管理メニュー配下）
  企業比較       → **`/compare`**（`/search` は301で転送）

3,661行 → 1,233行。⚠️ 切り出しのとき `:root` を取りこぼし、CSS変数6個が
未定義で入力欄が読めなくなった（`1c9dc04` で修正・テストで検知）。

⚠️ 旧編集画面は銘柄ページの `data-type`（`current_liabilities_list` /
`equity_ratio_list`）をそのままキーにしていたため、**流動負債と自己資本比率を
直しても画面に反映されなかった**。新しい画面は実キーを使う。

### 8. スクリーナーの絞り込み拡張（`4dcc128`）

よく使う10項目は常時表示、残りは「詳細な絞り込み」を開いたとき。
⚠️ **閉じていても条件は効く**ので、いくつ入っているかをボタンに出す。
⚠️ **AND固定。** ORは実務でほぼ使わず、UIが一気に難しくなる。

### 9. 増減率・流動比率の列を埋めた（`7ca80cd`）

成長率の列は**全銘柄で空**で「増収率10%以上」で探せなかった。
**外部アクセスなし・DB内の計算だけ**で3,800銘柄を更新。

⚠️ **列の名前とスコアの言い方がずれている。**
  cy=直近の確定決算 / 1y=1期前 / 2y=2期前 / ny=今期の会社予想
  スコア「2期前→前期」= `revenue_growth_1y_cy`
  スコア「前期→今期予」= `revenue_growth_cy_ny`
  対応は `analysis_quality.GROWTH_COLUMNS` の注記が正。5銘柄で突き合わせ済み。
  **違うと「スコアでは増収なのに増収率で絞ると出てこない」が起きる。**

⚠️ 派生値なので毎晩 `_recalculate_growth_columns()` で作り直す。既存25件は
まさに古くなっていた（JVCケンウッドの `cy` が2025-03期のまま）。

### 10. マーケット（`d8d66c8` `e0c4bbc`）

TOPIXを外した（連動ETF 1306 で代用しており「値そのものは指数値ではない」）。
金・WTI原油・米10年金利を足して8本。**4列×2段**に固定。

  1段目 日経平均 / ダウ / S&P500 / ドル円
  2段目 金 / 原油 / 米10年金利 / ビットコイン

⚠️ **代用品を指数として並べない**という線をテストで引いた。
⚠️ カードの列数(4)で割り切れる本数にする、もテストで固定。

### 11. チャートの日付が英語圏の並びだった（`e0c4bbc`）

lightweight-charts は `locale: 'ja-JP'` を渡しても**日付の書式は変わらない**。
カーソルのラベル（`localization.timeFormatter`）と横軸の目盛り
（`timeScale.tickMarkFormatter`）は**別々の指定**が要る。
`static/js/chart-date.js` に集約し、5つのチャートで同じ書式にした。

### 保留にしたもの：スコアに「有利子負債 ≤ 純利益×10」

| 定義 | 判定できる | 合格率 | 中央値 | 上位25% |
|---|---:|---:|---:|---:|
| 有利子負債÷純利益 ≤10 | 79.6% | 76.3% | 3.1年 | 9.3年 |
| 純有利子負債÷純利益 ≤10 | 75.8% | 86.9% | 0.0年 | 3.7年 |
| 純有利子負債÷営業CF ≤10 | 71.7% | 95.1% | 0.0年 | 1.9年 |

- **10倍という線は当たりが良い**（上位25%が9.3年＝4社に1社が落ちる）
- ⚠️ **致命的な穴**: 赤字だと `judged=False` になり**減点されない**。
  判定不能787件のうち **380件が「赤字なのに有利子負債あり」**
  （日産8.9兆/−6,709億、東電HD6.3兆/−4,543億、楽天G5.6兆/−1,779億、
  本田4.9兆/−4,239億）。**借金の重さを見る項目が、借金が一番重い会社で沈黙する**
- 入れるなら**赤字は「判定不能」ではなく「不合格」**に（PER項目に前例あり）
- ⚠️ **「12項目」がLP(7)・`/welcome`(6)・銘柄ページ(9)など10ファイルに埋まっている**

---

## 2026-08-24 の作業（プッシュ済み・デプロイ済み）

### 直したこと

**1. PER・PBR等が分析時のまま止まっていた**（`ff6a0fe`）

`stock_price` だけを毎日の cron が更新し、そこから計算される
PER・PBR・時価総額・配当利回りは分析した日のまま置かれていた。
銘柄ページに「今日の株価」と「1か月前のPER」が並んでいた。

実測（3,325銘柄で検算）: 5%以上ズレ 64% / 20%以上 31%。
例: 2477 手間いらず は 13.8倍 → 正しくは 18.2倍。

- `multiples.py` が伸縮の計算の唯一の正。**株価÷EPSで計算し直さないこと**
  （EPSは報告通貨。6269 三井海洋開発はドル建てで3,059倍という嘘が出る）
- 不変条件: per_forward / pbr / market_cap / dividend_yield は
  **同じ行の stock_price と同じ時点のもの**。`price_updated_at` がその印
- 本番DB適用済み: 3,778件 / 失敗0

**2. 決算銘柄の再分析が手動だった**（`1eaf6ec`）

検知は自動なのに再分析は /earnings のボタン頼み。毎晩22:00に自動化した。

### 残っていること

**未同期の102件**（`price_updated_at` が NULL のまま）

`backfill_multiples_sync.py` が「何倍になったか測れない」と判断して
触らなかった行。内訳は日足が古い58件・分析日が日足の範囲外40件・
株価がチャートと合わない数件（株式分割/併合の疑い）。

→ **再分析すれば直る**。下の「186件…」ではなくこちらが実際の残件。

```
python backfill_multiples_sync.py            # 状況を見るだけ
```

**再分析でしか直らないもの**

`updated_at` 列は一部の保存経路でしか書かれておらず、2月のまま止まっている
行が184件ある。**中身が古いわけではない**（`analyzed_at` は7〜8月）。
鮮度を見るときは `analyzed_at` を使うこと。`updated_at` を信じると誤診する。

**Yahoo項目のバックフィル**（毎晩2:00に60件ずつ自動で進む）

急ぐときは `bash _accel.sh`（400件×2＋score_complete再計算、約8時間）。
2026-08-24 06:23 と 17:16 に各800件完走。

---

## 2026-08-19 の作業（プッシュ済み・デプロイ済み）

### いま走っているもの（2026-08-20 夜）

**本番バックフィル**（`screened_latest` に書き込む）:

```
python backfill_yahoo_fields.py --max-per-run 400 --sleep 5.0
```

- ログ: `claudedocs/backfill_real_400.log`
- **未取得だけを拾うので、いつ止めても同じコマンドで続きから走る**
- 1銘柄18.3秒。400件で約2時間。**全3,800件だと約19時間**なので10回ほどに分けて流す
- 進捗: `grep -c "OK (" claudedocs/backfill_real_400.log`
- 件数確認: 下の「実行前後の件数」参照

**実行前の件数（2026-08-20）**

| | |
|---|---|
| forecast_revenue | 382 |
| forecast_op_income | 384 |
| business_summary_jp | 3,784 |

（50件テスト前は 348 / 350。50件で +34＝68%が埋まった）

⚠️ ブレーカーが開いたときの「待って復帰」はまだ本番で発動していない。
このログに「遮断されました。◯分待って再開します」が出たら、そこが初検証。

---

### 計測スクリプト（DBに書かない方）

```
python backfill_impact_dryrun.py --missing-only --spread 100 --sleep 1.5 --resume
```

- ログ: `claudedocs/dryrun_run3.log`
- 結果: `claudedocs/backfill_impact_dryrun.jsonl`（**1銘柄ずつ追記**。途中で落ちても残る）
- 中断したら**同じコマンドをもう一度**打てば続きから走る（`--resume` 付き）
- 途中経過だけ見たい: `python backfill_impact_dryrun.py --report`

**何を測っているか** ＝ いま今期予想が入っていない銘柄を、コード帯を散らして100件。
①本当に埋まるのか ②スコアがどう動くのか（PER/PBRが判定不能に転ばないか）。
これが出れば**全3,879件のバックフィルをやるかどうか**を決められる。

判断の目安：埋まる率が3割を切るなら全件（約4.5時間・1銘柄10リクエスト）は割に合わない。
EDINET DB（業績予想あり・Free枠100回/日）を時価総額順に日々消化する方に倒す。

### 触ったファイル（`git status` で確認できる）

| ファイル | 内容 |
|---|---|
| `yahoo_jp_guard.py` | **ブレーカーに半開放**（10分→倍々・上限60分）。以前は `reset()` を手で呼ぶまで戻らず、予想が全銘柄で取れない真因だった |
| `app.py` | `_enqueue_announced()`。**決算更新の二度押し／21時cronで全件再取得**になっていたのを修正。日付をUTC→JSTに |
| `models/root.py` | `/stock/<code>` に会社名が来たときコードへ寄せる。`/welcome` `/seminar` 追加 |
| `company_lookup.py` | 会社名→証券コードの解決（新規） |
| `templates/stock_not_found.html` | 空ページの代わりに理由と候補を出す（新規） |
| `templates/stock_detail.html` | **適合度に12項目メーター**。色＝点数／枠＝充足度に分離 |
| `templates/search.html` | Enterで候補未選択のとき入力文字列のまま飛んでいたのを修正 |
| `templates/welcome.html` `templates/seminar.html` | 知人向け・勉強会LP（新規） |
| `.gitignore` | `!static/images/seminar/*.png`（`*.png` に巻き込まれて本番だけ404になる） |
| `tests/test_company_lookup.py` `tests/test_yahoo_jp_guard.py` | 新規。全272件通過 |

詳細な経緯と数字は `../../../contexts/projects/gia/company_note.md` の「20」〜「25」。

### 分かっていること（結論を間違えないための要点）

- **キオクシア(285A)に予想が無いのは会社が出していないから**（`not_disclosed`）。バグではない
- 予想の充足率9%の主因は、**当初考えた「在庫が古い」ではない**。
  ブレーカー（修正済み）と、**Yahooにそもそも業績ページが無い（`no_data`）**の合わせ技
- スコア副作用は50件計測で **PER/PBR判定不能 0件**＝全件やっても点数は崩れない
- ⚠️ **`order('company_code')` の先頭から取ると偏る。** 先頭50件は56%が予想持ち（母集団9%）

---

次のセッションはここを見れば、何が終わっていて何が残っているか分かる。
取得元・欠損対策の詳細は `DATA_ACQUISITION.md`、全体像は `../CLAUDE.md`。
会員・課金の全体は `../../../contexts/projects/gia/membership_plans_stripe.md`。

## 2026-08-12 までの状態

---

## 状態

`main` = 本番。未pushゼロ。テスト185件。

2026-08-10〜12 で入れたもの（すべて本番反映済み）:

- 認証をGIA（キャンパス）の Supabase Auth へ統一
- パスワード再設定（`/forgot-password` → `/reset-password`）。実メール到達確認済み
- モバイル導線（ボトムタブにマイノート／☰をヘッダー右端へ）
- **会員ゲート**（ページ・API・銘柄ページ・コミュニティ）
- アカウント設定に会員種別を表示
- ヘッダーの折り返し修正（lg 1024px で切替）

---

## 運用側でやること

### 1. Supabase のメール文面を日本語にする — ✅ 2026-08-12 設定完了
GIAプロジェクトの Authentication → Emails → Reset Password。件名・本文を差し替える。
`{{ .ConfirmationURL }}` は必ず残す（これがリンクの実体）。

⚠️ **このテンプレートは Company Note 専用ではない。**
GIAプロジェクトの `auth.users` は Company Note とキャンパスで共通なので、
将来キャンパス側に再設定を付けたら同じ文面が飛ぶ。
→ 「Company Note のパスワード」と書かず、**GIAアカウント**と書く。

**件名**
```
【GIA】パスワード再設定のご案内
```

**本文（HTML）**
```html
<p>GIA アカウントのパスワード再設定のご依頼を受け付けました。</p>

<p>下のリンクを開いて、新しいパスワードを設定してください。</p>

<p><a href="{{ .ConfirmationURL }}">パスワードを再設定する</a></p>

<p>このリンクの有効期限は1時間です。期限が切れた場合は、
お手数ですが再度お手続きをお願いいたします。</p>

<p>お心当たりのない場合は、このメールを破棄してください。
パスワードは変更されません。</p>

<hr>
<p style="font-size:12px;color:#666;">
GIA<br>
Company Note　https://note.gia2018.com<br>
HIROGARU キャンパス　https://gia2018.com
</p>
```

### 完了したもの（2026-08-12 確認）

- ✅ **`STRIPE_PRICE_PREMIUM_LIVE` を ¥33,000 に戻した**（実地テストで ¥50 に
  一時差し替えていたもの。招待URLを配る前に戻せた）
- ✅ Stripe の `動作確認TEST` 商品をアーカイブ
- ✅ 移行で弾かれた2名へ連絡（`1008petalchime1119@` / `sakkun.i.0622@`）
- ✅ ヘッダーのタブレット幅の折り返し = **☰に集約する現状の挙動でよい**と判断
  （五島さん「変に折り返すくらいならハンバーガーメニューもいい感じ」）

---

## コードで残っていること

### ✅ PBR/PER の食い違いと配当利回りの桁違い（2026-08-12 コード修正済み・DB反映待ち）

**コードは修正済み。本番DBの既存値はまだ直っていない。**
反映手順は下の「DBに流す手順」。

#### 分かったこと（当初の見立てを1つ訂正している）

最初は「Yahooが誤り、割り算が正しい」と見て割り算側を正にしようとしたが、
**割り算側も壊れる**ことが分かったので方針を変えた。

| 銘柄 | 壊れている側 | 内容 |
|---|---|---|
| 3939 | Yahoo | `bookValue` 10.319（貸借対照表からは 97.97）→ PBR 48.65倍（正: 5.26倍） |
| 1773 | 割り算 | EPS・BPS系列が同じ倍率で小さい → PBR 48.7倍（Yahooの 1.20倍 がROEと整合） |

**ROEによる検算は使えない。** EPSとBPSが同じ倍率で狂うと ROE（＝EPS÷BPS）は
変わらないので、スケール誤りを検出できない。株数を経由しない
「時価総額 ÷ 純資産」なら検出できるが、**`equity` 列は全3,879件で空**。

→ どちらが正しいか機械的に決められない。**決められないものを決めたふりで出さない。**
2つが1.5倍以上食い違ったら値を持たせず「判定不能」にする。
スコアは判定できた項目数を分母にするので（§10）、判定不能は減点にならない。

#### 配当利回り（こちらは原因が確定していて一意に直る）

スクリーナーを配当利回り順に並べると40%超が並んでいた。原因は2つ。
- `dividendYield` は**常に%**（0.4 は 0.4%）。それを「0.5未満なら小数」と推測して
  100倍する分岐があり、**利回り0.5%未満の銘柄を軒並み100倍**していた
  （9720: 0.4% → 40% / 153A: 0.43% → 43%）
- `trailingAnnualDividendRate` は**分割調整されないことがある**
  （4918: 実際15円のところ150円 → 47.5%）

→ `ticker.dividends`（分割調整済みの支払い実績）の直近12か月合計÷株価を正とした。
無配は 0.0 でなく None（「出していない」と「0%」は違う）。

#### DBに流す手順（未実施）

```
py -3 backfill_derived_multiples.py --dry-run   # PER 272件 / PBR 214件が変わる
py -3 backfill_derived_multiples.py
py -3 backfill_dividend_yield.py --dry-run      # 27件中 18件を再計算・9件を不明に
py -3 backfill_dividend_yield.py
py -3 recalc_match_rates.py                     # PER/PBRは12項目に入るので必須
```

`backfill_derived_multiples.py` は外部アクセスなし。
`backfill_dividend_yield.py` は対象銘柄だけ（27件）Yahooを引く。

---

### （記録）当初の調査メモ

`/stock/3939` で発覚。**同じ銘柄ページの中で PBR が食い違う。**

| 表示場所 | 出所 | 3939 の値 |
|---|---|---|
| PBR推移グラフ | `financial_history.bps`（貸借対照表由来） | **5.3倍**（正しい） |
| 基準適合度カード | `screened_latest.pbr`（Yahoo `priceToBook`） | **48.65倍**（誤り） |

**原因＝Yahooの `bookValue` が日本株で当てにならない。**
3939: Yahoo `bookValue`=10.319 に対し、貸借対照表は
純資産46.49億 ÷ 発行済47,457,294株 = **BPS 97.97円** → PBR 5.26倍。
`Stockholders Equity` と `Ordinary Shares Number` から計算した値は
DBの `financial_history.bps`（97.97）とも一致する。**倍率は銘柄ごとにバラバラ**
（3939=9.5倍 / 3399=6.7倍 / 4970=4.7倍 / 4393=4.0倍）なので単位バグではない。

**なぜ補正が効かなかったか** ＝ `stock_analyzer._fill_missing_multiples()` は
**「Yahooが値を返さなかったとき」しか株価÷BPSで補完しない**（`if result.get(key) is not None: continue`）。
Yahooが"間違った値"を返した場合は素通りする。欠損だけを見ていて、異常値を見ていない。

**影響範囲**（恒等式 `PBR = PER × ROE` で全3,879件を照合）:
- **13銘柄**が「PBR≥10で不合格」だが、実際は10未満＝**合格のはずなのに減点されている**
  （3939 / 3399 / 4970 / 3628 / 6072 / 4397 / 4393 / 2164 / 4971 / 6016 / 4443 / 436A / 4712）
- PBR≥10で不合格になっている銘柄は全体で83件。うち13件が誤判定

**修正方針（未着手）**＝`financial_history.bps` から作れるなら**そちらを正とする**。
理由: ①開示資料由来で誰でも再現できる ②同じページのグラフが既にそれを使っており、
2つの出所を持つこと自体が矛盾の原因 ③Yahooの `priceToBook` は検証手段が無い。
Yahooの値は BPS が作れない銘柄のフォールバックに下げる。
直したら**再計算とスコアの再保存が要る**（§10 のスコア定義修正と同じ手順）。

⚠️ **PERも同じ構造**（`per_forward` は Yahoo の `trailingPE or forwardPE`）。
3939 は EPS からの計算と近いので今回は表面化しなかったが、同じ点検をすること。

### admin 判定のねじれ（2026-08-25 に一部解消）

**運用系22本は `admin_required_api` に置き換え済み**で、そちらは
`session['user_role']`（メール基準も拾う）を見る。以下は `role_required('admin')`
を使い続けている `/api/admin/*` の話として残る。

```
ページ /admin/users   models/root.py _require_admin()
                      → session['user_role']（GIA_ADMIN_EMAILS か app_users.role）
API   /api/admin/*    app.py @role_required('admin')
                      → get_current_user()['role'] = app_users.role のみ
```
メールが管理者でもDBの role が admin でないと、**ページは開けてAPIだけ403**。
＝管理画面は開くのに中身が空／エラーになる。権限不足に見えないぶん原因が分かりにくい。

**実害があるかの確認**: `note.gia2018.com/admin/users` でユーザー一覧が出るか。
出れば現運用では問題なし（DBの role も admin）。将来ほかの人を管理者にするとき顕在化する。

修正方針＝`role_required` もメール基準（`GIA_ADMIN_EMAILS`）を見る。gia-next 側もメール判定。

### kabutan 依存の差し替え検討

方針（2026-09-01 五島さん）＝**アプリに載せる値の取得元にはしない。
どうしようもないときの、人が確認するための参照に限る。**

**判断（2026-09-01 五島さん）＝完全な代替案ができるまでは、いまの運用でカバーする。**
下の3か所は自動で叩いており方針とはずれているが、止めると決算検知そのものが
落ちるため、代替が用意できるまでは現行のまま運用する。

代替の本命は TDnet（決算短信・業績予想修正）。EDINET公式APIの導入が済んだので、
次の候補として検討する。
- `gc_scraper.py:117` … kabutan の表から PER/PBR
- `app.py` の技術銘柄一覧 … `sc.get('pbr') or sig.get('pbr')` で画面に出ている
- `earnings_scraper.py` … 決算発表銘柄の検知そのもの

単純に消すと `_filter_stocks()` の ETF・REIT 除外が壊れる（PER/PBRで判定）。
**「消す」ではなく「`security_filter.py` か `screened_latest` の値に差し替える」**。
`screened_latest.pbr` は99%、`per_forward` は93%埋まっているので差し替え先は十分。

### 学習の進捗記録
実装・migration適用とも完了。実データでの通し確認だけ未了。

---

## 判断待ち（GIA側）

対象URL（すべて **gia2018.com** 側。Company Note ではない）:

| URL | ログイン | 用途 |
|---|---|---|
| `gia2018.com/plans` | 不要 | 料金比較の1枚。無料/¥4,980/¥7,980。配布用 |
| `gia2018.com/upgrade` | **必須** | 申し込み画面。未ログインは `/login` 経由で戻る |
| `gia2018.com/upgrade/invite` | 必須 | ¥11,000（非公開・URL直渡し） |
| `gia2018.com/upgrade/premium` | 必須 | ¥33,000（非公開・URL直渡し） |

- `/upgrade`・`/plans` のキャッチや説明文を五島さんの言葉にする
  （いま入っている文言は正本の定義から起こしたもので、間違ってはいないが
  セミナーで話している言い回しではない）。**課金ページなので売上への効きが一番大きい残件**
- ~~`invite` / `premium` の申込URLを配る運用~~ → **✅ 2026-08-12 決着＝現状のままでよい**。
  **承認ステップは無い**（URLを開くと即Stripe決済へ直行し、webhookが自動で段を付与。
  `noindex` だが転送されれば誰でも買える）が、**五島さん確認＝premium の会に定員なし**。
  定員が無いなら誤爆は返金で戻せるので、招待コードは作らない。
  ⚠️ **定員を設ける日が来たら、その時点で招待コードが必要になる**
  （席が埋まる損失は返金では戻せない）。

---

## この期間の教訓

- **段の名前を変えたら「その名前を読んでいる場所」を全部洗う。**
  webhook だけ直しても、画面側の判定が古い名前を見ていると
  「買ったのに使えない」になる（実際に3箇所で起きた）
- **ぼかしは装飾。保護はサーバーが値を送らないこと。**
- **会員/非会員で分岐する画面は、両方レンダリングして確認する。**
  目視は会員側しか見ないので、非会員だけ崩れていても気づけない
- 決済まわりの壊れ方は「お金を払った人が使えない」に出る

テストは `py -3 -m unittest discover -s tests` で185件。
