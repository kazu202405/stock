# Company Note project instructions

このリポジトリは Company Note（企業分析アプリ）の実装です。

## 最初に読むもの

1. `CLAUDE.md` — アプリ全体の目的・構成・設計方針
2. `claudedocs/DATA_ACQUISITION.md` — 株価・財務・日本語概要・株主・役員等の取得仕様と改善計画
3. 事業判断が必要な場合は `../../../contexts/projects/gia/company_note.md`

上記の調査日以降に大きな変更がある、実コードと記述が矛盾する、本番障害が再発した場合だけ追加調査する。毎回すべてのMarkdownや取得経路を再走査しない。

**ただしドキュメントを鵜呑みにしない。** 2026-08-08に `CLAUDE.md` の実装ステータス欄が
実態と食い違っていたことが判明した（「マイページ・コミュニティはフロントのみ」
「ログインは何でも通る仮実装」と書かれていたが、いずれも実際には本番稼働していた）。
機能の有無を判断材料にするときは、DBのテーブル実在と `app.url_map` を1回確認する。
確認した結果が記述と違ったら、その場でドキュメントを直す。

## データ取得で守ること

- `取得失敗`、`未収録`、`処理を省略`、`値がゼロ`を区別する。
- 外部取得が空だった場合、DBにある以前の正常値を空値で上書きしない。
- 取得結果は `source_status` に取得元・状態・理由を残す。
- Yahoo!ファイナンス日本版はHTML取得であり、安定APIとして扱わない。
- 四季報オンラインの記事・特色を取得、保存、再配信する処理は追加しない。既存の旧フォールバックは撤去対象。
- 公式情報は EDINET、JPX、TDnet、会社IRの順で検討し、出典日と基準日を保存する。
- スコアでは欠損を不合格扱いにしない。判定項目数とデータ充足度を必ず併記する。
- DB migration は `supabase/` 配下に既存ファイルの続きとして追加する。

## 確認

主要な回帰確認は次で実行する。

```powershell
py -3 -m unittest discover -s tests -v
```
